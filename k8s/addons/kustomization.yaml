apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # MetalLB (native manifests)
  - https://raw.githubusercontent.com/metallb/metallb/v0.14.5/config/manifests/metallb-native.yaml

  # Longhorn (CRDs + controllers; adjust version if needed)
  - https://raw.githubusercontent.com/longhorn/longhorn/v1.6.2/deploy/longhorn.yaml

  # Kubernetes Dashboard
  - kubernetes-dashboard

