# Copy this file to terraform.tfvars and customize for your environment
# cp terraform.tfvars.example terraform.tfvars

# Kubernetes Configuration
kubeconfig_path = "~/.kube/config"

# kube-vip Configuration (DHCP LoadBalancer)
kube_vip_version   = "v0.6.4"    # kube-vip container version
kube_vip_interface = "eth0"      # Network interface (usually eth0, ens160, or enp0s3)

# Security Configuration
baget_api_key           = "your-very-secure-api-key-here-generate-a-random-one"
grafana_admin_password  = "your-secure-grafana-password"

# Storage Configuration
longhorn_replica_count     = 2
baget_storage_size        = "10Gi"
prometheus_storage_size   = "20Gi"
grafana_storage_size      = "10Gi"
alertmanager_storage_size = "5Gi"

# Resource Configuration
baget_memory_request = "256Mi"
baget_memory_limit   = "512Mi"
baget_cpu_request    = "250m"
baget_cpu_limit      = "500m"

# Chart Versions (update as needed)
flannel_version                   = "v0.27.2"           # Flannel container version
flannel_cni_plugin_version        = "v1.7.1-flannel2"  # CNI plugin binary version
flannel_network_cidr              = "10.244.0.0/16"    # Pod network CIDR
longhorn_chart_version            = "1.6.2"
prometheus_stack_chart_version    = "57.2.0"
kubernetes_dashboard_chart_version = "7.13.0"

# Metrics Server (recommended for kubectl top and HPA)
metrics_server_enabled = true

# Kubernetes Dashboard Configuration (homelab-optimized)
dashboard_enable_skip_login      = true     # Skip login button (convenient for homelab)
dashboard_enable_insecure_login  = true     # Allow HTTP access (less secure but convenient)
dashboard_session_lifetime       = 43200    # Session timeout in seconds (12 hours)
dashboard_auto_generate_certificates = true # Auto-generate TLS certificates

# Router DNS Setup (after deployment):
# 1. Deploy your services and check router DHCP client list for assigned IPs
# 2. Pin/reserve those IPs in router DHCP settings  
# 3. Add DNS A records on router: 
#    - grafana.k8s.home -> grafana_ip
#    - dashboard.k8s.home -> dashboard_ip
#    - longhorn-ui.k8s.home -> longhorn_ip
#    - prometheus.k8s.home -> prometheus_ip
#    - baget.k8s.home -> baget_ip


# Labels
common_labels = {
  "managed-by"  = "terraform"
  "environment" = "homelab"
  "cluster"     = "main"
}
